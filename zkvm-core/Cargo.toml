[package]
name = "zkvm-core"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/vm.rs"

[features]
default = []

[library.dependencies]
rv32im-decoder = { path = "../rv32im-decoder", default-features = false }
