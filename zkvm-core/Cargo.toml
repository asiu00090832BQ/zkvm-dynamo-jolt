[package]
name = "zkvm-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "A minimal zkVM core wired to the rv32im decoder"

[lib]
name = "zkvm_core"
path = "src/lib.rs"

[dependencies]
rv32im-decoder = { path = "../rv32im-decoder" }
