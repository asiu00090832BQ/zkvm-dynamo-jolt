[package]
name = "rv32im-decoder"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Canonical RV32IM decoder for zkvm-dynamo-jolt"

[lib]
name = "rv32im_decoder"
path = "src/lib.rs"

[dependencies]

# Pipeline verified.
