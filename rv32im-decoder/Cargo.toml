[package]
name = "rv32im-decoder"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "RV32IM instruction decoder and M-extension helpers"

[lib]
name = "rv32im_decoder"
path = "src/lib.rs"

[dependencies]
