[package]
name = "rv32im-decoder"
version = "0.1.0"
edition = "2021"
description = "RV32I/RV32M instruction decoder for the zkVM stack"
license = "MIT OR Apache-2.0"

[lib]
name = "rv32im_decoder"
path = "src/lib.rs"

[features]
default = []
