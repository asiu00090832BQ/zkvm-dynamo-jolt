# zkvm-dynamo-jolt

This project implements a zkvM running a variant of Dynamo with a RISC-V ISA. It combines Jolt Sumcheck lookup optimizations with Zeroos memory management.

DIRECTIVE: Build and push to a github repo.

- Rust vm
- JS tests (optional)
- Barebones wrapper for verification
- Technical description